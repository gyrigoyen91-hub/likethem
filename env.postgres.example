# Postgres Database URLs (Supabase/Neon)
# Replace with your actual connection strings

# Pooled connection (for serverless/Vercel)
DATABASE_URL="postgres://<user>:<pass>@<host>:6543/postgres?pgbouncer=true&connect_timeout=15&sslmode=require"

# Direct connection (for migrations and direct access)
DIRECT_URL="postgres://<user>:<pass>@<host>:5432/postgres?sslmode=require"

# NextAuth Configuration
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-nextauth-secret-here"

# Google OAuth
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"
